<?php

/* 
    On appelle modèle ce qui l'ensemble des données d'une application et les opérations qui s'y rapportent.
    Indépendamment de l'usage qu'on en fait.

    Vous aurez remarqué que toutes les données json on été regroupées dans un seul et même fichier
    appelé data.json. Gérer un fichier par pièce n'est pas bien pratique ni réaliste si on avait énormément
    de pièces.

    Par contre cela implique que si l'on veut les données sur une pièce, il faut aller les chercher dans le
    fichier data.json.

    Complétez la fonction getRoomData en vous aidant des commentaires. Il y a une instruction pour chaque ligne de commentaire.
    
*/


/*  getRoomData

    . paramètre $d : le nom d'une pièce
    > valeur de retour : un objet standard contenant les données sur la pièce $d
*/
function getRoomData( $d ){
    // charger le contenu du fichier "data.json" dans une variable $json par exemple
    // TODO
    // transformer le contenu json en objet PHP standard (https://www.php.net/manual/fr/function.json-decode.php)
    // TODO
    // retourner les données sur la pièce $d
    // TODO
}
